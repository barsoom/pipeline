dependencies:

  # Fix issues until we can upgrade to CircleCI 2.0.
  pre:
    - sudo ln -s /home/ubuntu /home/travis

test:
  override:
    - bundle exec rake

deployment:
  deploy:
    branch: master
    commands:
      - sh tmp/deploy.sh
